version: '3.8'
services:
  dashboard:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - GWD_DB_PATH=/data/gwd.db
      - GWD_ENCRYPTION_KEY=${GWD_ENCRYPTION_KEY}
    volumes:
      - gwd_data:/data
    restart: unless-stopped

volumes:
  gwd_data:
